{# {% block faq_list %}
    <sw-page class="faq-list">
        {% block faq_list_smart_bar_actions %}
            <template #smart-bar-actions>
                <sw-button variant="primary" :routerLink="{ name: 'faq.create' }">
                    {{ $tc('faq.list.addButtonText') }}
                </sw-button>
            </template>
        {% endblock %}
        <template #content>
            {% block faq_list_content %}
                <sw-entity-listing
                    v-if="faqs"
                    :items="faqs"
                    :repository="repository"
                    :showSelection="false"
                    :columns="columns"
                    detailRoute="faq.detail">
                </sw-entity-listing>     
            {% endblock %}
        </template>
    </sw-page>
{% endblock %} #}